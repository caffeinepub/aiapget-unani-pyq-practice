{
  "frontend/src/hooks/useQueries.ts": "React Query hooks for reading and writing quiz attempt history using localStorage (since the backend no longer has array storage methods), with `useQuizHistory`, `useRecordAttempt`, and `useClearHistory` hooks.",
  "frontend/src/data/questions.ts": "Expanded static question bank with 140 AIAPGET Unani PYQ MCQs (up from 60) distributed across all 9 topics and years 2016–2025; each question has id, text, four options, correctIndex, topic, year, and optional explanation. The YEARS constant is also updated to include 2024 and 2025.",
  "frontend/src/App.tsx": "Root app component managing screen navigation state across all screens including new `subscription` and `payment` screens; renders the persistent `AppHeader` on all non-quiz screens and wraps everything in `QueryClientProvider`.",
  "frontend/src/pages/HomeScreen.tsx": "Home screen without its own header (now handled by AppHeader), showing hero banner, stats, navigation cards including a new Subscription Plans card with Premium badge, and footer.",
  "frontend/src/pages/QuizScreen.tsx": "Interactive MCQ quiz screen with per-question 60-second countdown timer; updated to ensure all option text uses explicit `text-foreground` on unselected states, correct options show `text-success font-bold`, incorrect selections show `text-destructive font-medium`, and the timer warning uses a solid `bg-red-600 text-white` for clear readability.",
  "frontend/src/pages/ResultsScreen.tsx": "Quiz results screen with improved text contrast — topic breakdown labels and counts use `text-foreground font-medium/semibold` instead of muted colors, skipped stat uses `text-foreground`, and the \"correct out of total\" summary uses `font-medium` for better readability.",
  "frontend/src/pages/ReviewScreen.tsx": "Answer review screen with fixed text contrast — correct options show `text-success font-bold`, incorrect user-choice options show `text-destructive font-semibold`, unselected options use explicit `text-foreground`, and the option letter badge uses `text-foreground` instead of `text-muted-foreground` for better visibility.",
  "frontend/src/pages/TopicBrowserScreen.tsx": "Topic and year browser screen using `useAllQuestions` to merge static and backend questions; the Quiz button in the \"Browse by Year\" section now uses the same `bg-primary hover:bg-primary/90` styling as the Quiz button in the \"Browse by Topic\" section for visual consistency.",
  "frontend/src/pages/HistoryScreen.tsx": "Score history screen with improved text contrast — empty state message uses `text-foreground`, grade labels use `font-semibold`, date/score details use `font-medium`, and the clear button uses `text-primary-foreground/80` instead of `text-white/70` for semantic correctness.",
  "frontend/src/index.css": "Global styles with Playfair Display + Inter fonts and an updated eye-friendly OKLCH color system — softer warm ivory background (L=0.94), deep ink foreground (L=0.16) for high contrast, muted teal primary (L=0.34), darkened gold (L=0.58) and teal (L=0.44) for legible text, and darker success/destructive tokens ensuring 4.5:1+ contrast ratios throughout.",
  "frontend/tailwind.config.js": "Tailwind config extending the design system with Playfair Display heading font, Inter body font, and all color tokens mapped to the updated OKLCH CSS variables for eye-friendly accessible contrast across all utility classes.",
  "frontend/src/hooks/useAdminQueries.ts": "React Query hooks for admin operations — `useGetAdminQuestions` (with retry:false to avoid repeated auth errors), `useAddQuestion` (with suppressed onError to let the component handle filtering), and `useAllQuestions` (merges backend and static questions with retry:false).",
  "frontend/src/pages/AdminPanelScreen.tsx": "Password-gated Question Manager screen that resets mutation state and clears error/success messages on mount and tab change; filters out any backend authorization error messages from being displayed to the user, while keeping the password gate and full Add Question form with explanation field and dynamic year dropdown.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor using the current Internet Identity (if any), returning the actor and a loading flag; removes the non-existent `_initializeAccessControlWithSecret` call that was causing TypeScript build failures.",
  "frontend/src/hooks/useSubscriptionQueries.ts": "React Query hook `useGetSubscriptionPlans` that fetches subscription plans from the backend actor using the `getSubscriptionPlans` query method.",
  "frontend/src/components/AppHeader.tsx": "Persistent app header with a 3-dot vertical kebab menu (MoreVertical icon) in the top-left corner that opens a dropdown navigation drawer linking to all main screens including Subscription Plans; dismissible by clicking outside.",
  "frontend/src/pages/SubscriptionPlansScreen.tsx": "Subscription plans screen showing Monthly (₹199/mo) and Yearly (₹1,499/yr, \"Save 37%\") plan cards with feature lists, a toggle switcher, Best Value badge on yearly, and Subscribe buttons that navigate to the payment selector.",
  "frontend/src/pages/PaymentMethodSelectorScreen.tsx": "Payment method selector UI (mock only) showing UPI (recommended, with badge), Credit Card, Debit Card, Net Banking, and Wallets with dynamic input fields per method, a mock Pay button with loading state, and a success confirmation screen.",
  "frontend/index.html": "HTML entry point updated with full PWA support — includes manifest link, theme-color meta tag, iOS-specific PWA meta tags (apple-mobile-web-app-capable, status-bar-style, title, touch-icon), and an inline service worker registration script.",
  "frontend/public/manifest.json": "Web App Manifest for PWA installation with app name, short name, standalone display mode, parchment background color, deep teal theme color, and references to both 192×192 and 512×512 PNG icons.",
  "frontend/public/sw.js": "Service worker implementing cache-first strategy for static assets and network-first for ICP canister/API calls, with app shell pre-caching on install, old cache cleanup on activate, and graceful offline fallback to cached index.html for SPA navigation."
}